import { Link } from '@tanstack/react-router';
import { useState } from 'react';
import { getUser } from '../api/client.ts';
import { useFiscalBoard, type FiscalBoard } from '../api/fiscal.ts';
import { useMeta } from '../api/queries.ts';
import { useManagementPortal, type PayrollSummary } from '../api/workforce.ts';
import { useWorkforceTask } from '../api/workforce-query.ts';
import { FiscalConditions } from '../components/fiscal-conditions.tsx';
import { FiscalPayrollCalculate, FiscalPayrollEvidence } from '../components/fiscal-payroll.tsx';
import { FiscalYearEnd } from '../components/fiscal-year-end.tsx';
import { WorkforcePayroll } from '../components/workforce-payroll.tsx';
import { ReadRecoveryProvider, ReadRefreshNotice } from '../components/read-refresh-notice.tsx';
import { WorkforceHero, WorkforceTabs } from '../components/workforce-shell.tsx';
import { WorkforceEmpty, WorkforceError, WorkforcePanel } from '../components/workforce-shared.tsx';
import { useLocale } from '../i18n.tsx';
import { businessToday } from '../lib/operations.ts';
import { canRetainData } from '../lib/read-recovery.ts';
import { LoadingView } from './status-views.tsx';
import '../fiscal.css';
const lastMonth = () => { const date = new Date(businessToday() + 'T00:00:00Z'); date.setUTCDate(1); date.setUTCMonth(date.getUTCMonth() - 1); return date.toISOString().slice(0, 7); };
const tabs = [{ id: 'conditions', label: { ja: '税・保険の条件', en: 'Tax and insurance' }, icon: 'people' as const }, { id: 'payroll', label: { ja: '給与の算定', en: 'Payroll' }, icon: 'wallet' as const }, { id: 'year-end', label: { ja: '年末調整', en: 'Year-end' }, icon: 'document' as const }, { id: 'evidence', label: { ja: '支払の証跡', en: 'Payment evidence' }, icon: 'check' as const }];
export function FiscalPage() {
  const { t } = useLocale(), meta = useMeta(), task = useWorkforceTask();
  const [tab, setTab] = useState('conditions'), [period, setPeriod] = useState(lastMonth), [employeeId, setEmployeeId] = useState('');
  const [calculate, setCalculate] = useState<{ employee: FiscalBoard['employees'][number]; original?: PayrollSummary }>(), [evidence, setEvidence] = useState<PayrollSummary>();
  const actions = meta.data?.actions.map((action) => action.name) ?? [], allowed = actions.includes('workforce.fiscal_board'), fiscal = useFiscalBoard(allowed), payroll = useManagementPortal(period, allowed);
  if (!meta.data && !meta.isError) return <LoadingView />;
  const failed = [meta, fiscal, payroll].find((source) => source.isError && !canRetainData(source));
  if (failed) return <div className="workspace-page workforce-page"><WorkforceError error={failed.error} onRetry={() => { void meta.refetch(); void fiscal.refetch(); void payroll.refetch(); }} /></div>;
  if (!allowed) return <div className="workspace-page workforce-page"><p role="alert">{t({ ja: '税・保険と年末調整は給与本部の権限が必要です。', en: 'Payroll headquarters permission is required for tax, insurance and year-end adjustment.' })}</p></div>;
  if (!fiscal.data || !payroll.data) return <LoadingView />;
  const data = fiscal.data, payrolls = payroll.data.payrolls, selected = data.employees.find((employee) => employee.id === employeeId), visible = payrolls.filter((row) => !selected || row.employeeId === selected.id), selfEmployeeId = payroll.data.employees.find((employee) => employee.userId === getUser()?.id)?.id;
  const prior = selected ? payrolls.find((row) => row.employeeId === selected.id && row.status !== 'cancelled') : undefined;
  return <ReadRecoveryProvider sources={[meta, fiscal, payroll]}><div className="workspace-page workforce-page" data-testid="fiscal-management"><ReadRefreshNotice /><WorkforceHero title={t({ ja: '給与の根拠を、ひとつにつなぐ。', en: 'Payroll with a clear chain of evidence.' })} description={t({ ja: '本人条件、月次の税・保険、年末調整と実精算。確認する場所がわかる給与本部のワークスペース。', en: 'Employee conditions, monthly tax and insurance, annual adjustment and settlement. A payroll workspace that makes each review clear.' })} management side={<><strong>2026</strong><span>{t({ ja: '国内給与・年末調整', en: 'Japanese payroll and year-end' })}</span></>} /><div className="fiscal-toolbar"><label>{t({ ja: '従業員', en: 'Employee' })}<select aria-label={t({ ja: '従業員', en: 'Employee' })} className="input" value={selected?.id ?? ''} onChange={(event) => setEmployeeId(event.target.value)}><option value="">{t({ ja: '全従業員', en: 'All employees' })}</option>{data.employees.map((employee) => <option value={employee.id} key={employee.id}>{employee.code} · {employee.name}</option>)}</select></label><Link className="btn" to="/workforce">{t({ ja: '勤怠・従業員管理へ', en: 'Attendance and people' })}</Link>{actions.includes('workforce.work_system_board') ? <Link className="btn" to="/workforce/systems">{t({ ja: '勤務制度を確認', en: 'Review working-time systems' })}</Link> : null}</div>{!data.rules.length ? <WorkforcePanel title={t({ ja: '給与制度資料の準備', en: 'Prepare payroll rule data' })} icon="document"><p>{t({ ja: '2026年の一次資料に基づく税・保険の制度値と参照元をこの会社に登録します。', en: 'Register 2026 tax and insurance values and their primary sources for this company.' })}</p>{task.error ? <WorkforceError error={task.error} /> : null}<button className="btn btn-primary" disabled={task.isPending} onClick={() => task.mutate({ action: 'workforce.initialize_payroll_rules', input: {} })}>{t({ ja: '2026年の制度資料を準備', en: 'Initialize 2026 rule sources' })}</button></WorkforcePanel> : <details className="workforce-notice"><summary>{t({ ja: '適用する制度資料と出典', en: 'Rule versions and primary sources' })}</summary>{data.rules.map((rule) => <div key={rule.id}><strong>{rule.code}</strong><ul>{rule.sources.filter((url) => url.startsWith('https://')).map((url) => <li key={url}><a href={url} target="_blank" rel="noreferrer">{url}</a></li>)}</ul></div>)}</details>}<WorkforceTabs tabs={tabs} selected={tab} onSelect={setTab} />{tab === 'conditions' ? <FiscalConditions data={data} employeeId={selected?.id ?? ''} /> : null}{tab === 'year-end' ? <FiscalYearEnd data={data} employeeId={selected?.id ?? ''} {...(selfEmployeeId ? { selfEmployeeId } : {})} /> : null}{['payroll', 'evidence'].includes(tab) ? <><div className="fiscal-toolbar"><label>{t({ ja: '給与対象月', en: 'Payroll month' })}<input className="input" type="month" value={period} onChange={(event) => { if (/^\d{4}-(0[1-9]|1[0-2])$/.test(event.target.value)) setPeriod(event.target.value); }} /></label>{tab === 'payroll' ? <button className="btn btn-primary" disabled={!selected || !data.rules.length || prior?.status === 'confirmed'} onClick={() => { if (selected) setCalculate({ employee: selected, ...(prior ? { original: prior } : {}) }); }}>{t({ ja: '選択した従業員の税・保険込みで算定', en: 'Calculate selected employee with tax and insurance' })}</button> : null}</div>{tab === 'payroll' ? <WorkforcePayroll rows={visible} actions={actions} {...(selfEmployeeId ? { selfEmployeeId } : {})} /> : <WorkforcePanel title={t({ ja: '確定給与の支払証跡', en: 'Confirmed payroll payment evidence' })} icon="document">{visible.filter((row) => row.status === 'confirmed').map((row) => { const found = data.taxEvidence.find((item) => item.payrollId === row.id); return <article className="workforce-record" key={row.id}><h3>{row.employeeName} · {row.period}</h3>{found ? <><p>{found.paymentDate} · {t({ ja: '課税支給', en: 'Taxable pay' })}: {found.taxablePay}</p><p>{found.basis}</p></> : <button className="btn" disabled={row.employeeId === selfEmployeeId || Boolean(row.calculation.statutory)} onClick={() => setEvidence(row)}>{t({ ja: '原資料から支払証跡を登録', en: 'Add payment evidence from source records' })}</button>}</article>; })}{!visible.some((row) => row.status === 'confirmed') ? <WorkforceEmpty>{t({ ja: '対象月に確定給与がありません。', en: 'No confirmed payroll for this month.' })}</WorkforceEmpty> : null}</WorkforcePanel>}</> : null}{calculate ? <FiscalPayrollCalculate employee={calculate.employee} period={period} original={calculate.original} current={payrolls.find((row) => row.employeeId === calculate.employee.id && row.status !== 'cancelled')} onClose={() => setCalculate(undefined)} /> : null}{evidence ? <FiscalPayrollEvidence row={evidence} onClose={() => setEvidence(undefined)} /> : null}</div></ReadRecoveryProvider>;
}
