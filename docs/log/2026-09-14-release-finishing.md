# 追加レビューの仕上げ（2026-09-14）

- 仕様: [release-finishing](../specs/release-finishing.md)
- 開始commit: `669328371e3ab910ebf189ca1f2e5ea1a8debe45`
- branch: `feat/release-finishing`（source-consistencyの後続）
- 状態: 実装・ローカル受入済み。最終commitの全gateと各CIは対象PRのChecksを正本として判定する。

## 現行ソースとの照合と判断

| 指摘 | 現行の確認結果 | 今回の判断 |
| --- | --- | --- |
| Caddyのセキュリティヘッダー | nosniff/no-referrerは存在するがCSPは未設定。印刷用HTMLにはinline handlerがある | 本番profileへCSPを追加し、印刷の必要な処理を外部scriptへ移す。許可/拒否の両方を実ブラウザで確認する |
| ハッシュ形式と非同期化 | コスト付き形式、非同期scrypt、旧形式検証/CAS更新、待ち行列上限は実装済み | 方式の重複変更はしない。setupだけが256文字を許す不整合を認証契約の200文字へ揃え、更新前の拒否を検証する |
| 仕訳転記のN+1 | before_submitは勘定を一括取得するが、after_submitは分類の読出しを明細ごとに反復する | 各フェーズ内で勘定をまとめて取得する。更新・参照整合性・lock・監査のための各行処理は保つ |
| ソースの実NUL | 管理対象のテキスト2ファイルに計4個。印刷可能でない制御文字が文字列リテラルへ直接入っていた | 明示エスケープへ置き換え、実行時の区切り・添付のバイナリ照合は変えない |
| チェーン運営への位置づけ | 多業種の共通基盤に、本部/店舗/従業員の業務が集積している | READMEに最初に試す対象と導線を追記し、多業種拡張の目的は維持する |
| 第三者によるpack追加 | 自動試験と拡張手順はあるが、人間の第三者が手順だけで完走した記録はない | 未検証と明記し、開始commit・業務台本・困った箇所・試験結果・PRを残す場所を既存手順へ追加する |

## 検証結果

### 仕訳とソースの実測

同じ2勘定を使う `postFromSource` を、専用PostgreSQLの非特権アプリ接続で実行し、ドライバが送るSQLを計数した。
「分類読出し」は勘定のtype等を含む行SELECT。「全SQL」にはトランザクション制御等も含み、書込はINSERT/UPDATE/DELETEを数える。

| 明細数 | 全SQL（前→後） | 全SELECT（前→後） | 勘定の分類読出し（前→後） | 書込（前→後） |
| --- | --- | --- | --- | --- |
| 2 | 68 → 68 | 54 → 54 | 3 → 2 | 12 → 12 |
| 20 | 338 → 320 | 288 → 270 | 21 → 2 | 48 → 48 |

分類取得の件数照会は1→2回へ変わる。全体のDB処理を定数回にしたわけではない。各明細の参照整合性・更新・lock・監査は維持した。
上限500明細の追加実測では、全SQL 7,040、SELECT 6,030、書込1,008、分類読出し2回/件数照会2回、貸借各25,000、500明細全ての転記を確認した。500明細の修正前比較や同時負荷は測定していない。

- 新規DB回帰8件: 2/20明細の実SQL、read ruleで隠した勘定、不存在/別会社/別tenant参照、取引先必須、3行目hook失敗時の原子性、監査/直接取消拒否/勘定変更後の逆仕訳、検証後hookで勘定が変わった際の再読込。
- 会計・消費税集計・販売・購買・入出金・在庫・添付の8ファイル75件が成功。黄金台帳/帳票とNULを含む添付のバイト往復を含む。
- 実NUL4個と隣接するSOH/STXはソースの明示エスケープに置き換えた。TypeScriptの文字列リテラル値を含む構文比較で、両ファイルの実行時の意味が同じことを確認した。画像等のバイナリは変更しない。
- 全単体123ファイル1,021件、全体の型検査が成功。setup/password/queueの既存回帰と新境界を含む。
- 別の新規PostgreSQLでsetup実受入が成功。201文字の指定passwordをbackup/migration前に拒否し、backup未作成・表/移行履歴なしを確認した。既存の初回導入・資格情報を保つ再開・データ付き0008からの更新・別DBへの実復元・移行失敗からの再開・使用済み復元先の拒否も通過した。

### 配信と最終確認

- Caddy 2.11.4とコンパイル済みWebを、検証用CAを信頼したChromiumで実配信し、cloud/onprem両profileの受入が成功した。証明書検証を無効化せず、CAを指定しないHTTPS要求は失敗することも確認した。
- ログインUIと同一origin API、深いURL、実Swagger資産/仕様、コンパイル済みピボット/シフトWorker、QR、実際の印刷イベント/閉じる・blobダウンロード、SSOへの画面遷移を確認した。印刷と保存は本物のヘルパーをブラウザで実行し、印刷画面のopener参照も残らない。
- 同一originから配信した試験scriptがinline script・イベント属性・eval・外部script/接続・form・base・objectの禁止操作を試みる。7種類のCSP違反と外部受信0を確認し、外部からのframe埋込、blob印刷ページのinline scriptも拒否された。DevToolsのコード実行をCSP拒否の証拠にしていない。
- 印刷/ダウンロードの単体2ファイル7件、配布全16件、最終format/typeが成功。固定Caddy archiveのSHA-512とworkflowを別担当が検証し、actionlintが成功した。
- 最終lintはエラー0・従来のサイズ警告4件、依存境界は1,259 module/6,206 dependencyで違反0。文書116件/ローカル参照1,089件と差分検査、staged秘密情報検査が成功した。
- 外部HTMLから読み込む `print-controls.js` はJS importのない正規のentryなので、依存検査では当該1ファイルをentry一覧へ明記した。HTMLからの読込と動作を実ブラウザで検証し、無関係な孤立ファイルの警告は維持する。
- 最終commitの全gate、通常/認証ブラウザ、setup、native edgeサービス、mutation/有限モデルと新CSP workflowは公開CIを最終判断に使う。ソースへの修正、合成環境の受入、実導入先の運用認定を区別する。

### 試験環境の隔離に関する修正

初回のCSP受入harnessはCaddyのHOME/XDG保存先を継承しており、ローカルの `~/.config/caddy/autosave.json` と `~/.local/share/caddy/last_clean.json` を更新した。サービスの導入・`/etc`の設定・AWSは変更していない。2ファイルはそれ以上操作せず保全し、試験ではvalidate/run双方のHOME/XDGを一時領域へ隔離する。修正後の再実行で元の2ファイルのstat/hash不変を確認し、この初回の影響を「全ての書込が最初から隔離されていた」とは扱わない。

## 対象外と引継ぎ

AWS試用環境・実DB・実機・実IdPは変更しない。Cookie認証と第三者による新規pack追加は未実装/未検証のまま保つ。
最終commitの全gateと各CIが成功するまで、PRを完了扱いにしない。照会削減の測定を運用容量の保証とは扱わない。
