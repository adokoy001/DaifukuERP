import { defineEntity, f, label, type Infer } from '@daifuku/kernel';
import type { FilingPrepared, FilingSource } from './profile.ts';
const owned = { serverOwned: true } as const;
const rights = ['read', 'create', 'update', 'export'] as const;
const profileFields = { key: f.text({ ...owned, required: true, immutable: true, unique: true }), data: f.json<Record<string, unknown>>({ ...owned, required: true }) };
export const FilingAccountingProfile = defineEntity({ name: 'filing_accounting_profile', label: label('財務諸表の準備設定', 'Financial statement preparation profile'), ext: false, fields: profileFields, permissions: { roles: { accounting: rights } }, views: { list: ['key'] } });
export const FilingPayrollProfile = defineEntity({ name: 'filing_payroll_profile', label: label('給与申告の補足情報', 'Payroll filing supplementary facts'), ext: false, fields: profileFields, permissions: { roles: { workforce_payroll: rights } }, views: { list: ['key'] } });
const packFields = { countryProfile: f.text({ ...owned, required: true, immutable: true }), from: f.date({ ...owned, required: true, immutable: true }), to: f.date({ ...owned, required: true, immutable: true }), fiscalYearId: f.ref('fiscal_year', { ...owned, immutable: true }), taxYear: f.int({ ...owned, immutable: true }), previousId: f.uuid({ ...owned, immutable: true }), idempotencyKey: f.uuid({ ...owned, required: true, unique: true, immutable: true, hidden: true }), request: f.json({ ...owned, required: true, immutable: true, outputHidden: true }), source: f.json<FilingSource>({ ...owned, required: true, immutable: true, outputHidden: true }), prepared: f.json<FilingPrepared>({ ...owned, required: true, immutable: true, outputHidden: true }), sourceHash: f.text({ ...owned, required: true, immutable: true, maxLength: 64 }), status: f.enum(['draft', 'confirmed', 'cancelled'], { ...owned, required: true, default: 'draft' }), preparedBy: f.uuid({ ...owned, required: true, immutable: true }), confirmedBy: f.uuid(owned), confirmedAt: f.timestamp(owned), reviewReason: f.text({ ...owned, maxLength: 2000 }) };
export const FilingAccountingPack = defineEntity({ name: 'filing_accounting_pack', label: label('会計申告準備の履歴', 'Accounting filing preparation history'), ext: false, fields: packFields, permissions: { roles: { accounting: rights } }, views: { list: ['from', 'to', 'status', 'sourceHash'] } });
export const FilingPayrollPack = defineEntity({ name: 'filing_payroll_pack', label: label('給与申告準備の履歴', 'Payroll filing preparation history'), ext: false, fields: packFields, permissions: { roles: { workforce_payroll: rights } }, views: { list: ['taxYear', 'status', 'sourceHash'] } });
export type FilingPack = Infer<typeof FilingAccountingPack>;
