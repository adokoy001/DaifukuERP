# Security policy

Daifuku は開発中の実験的な業務システムです。セキュリティ修正の対象は原則として最新の `main` です。現在、長期保守版や過去コミットへのバックポート、応答時間の保証はありません。実運用前には導入環境に合わせた権限・バックアップ・復元・アクセス制御の検証が必要です。

## 脆弱性の非公開報告

2026-09-12 に GitHub の非公開脆弱性報告を有効化し、APIで有効状態を確認しました。機密性のある問題は [非公開報告フォーム](https://github.com/adokoy001/DaifukuERP/security/advisories/new) へ送ってください。公開Issueへ事前に連絡先を問い合わせる必要はありません。

公開 Issue、Pull Request、Discussion に攻撃手順、認証情報、個人情報、実際の業務データを投稿しないでください。非公開で受け取った内容を確認し、影響範囲・修正・公開時期を報告者と調整します。

分かる範囲で、次を非公開フォームに記載してください。

- 影響するコミットまたはバージョン、Node.js / PostgreSQL のバージョン
- 自分で管理する検証環境と合成データによる最小再現手順
- 期待する動作と実際の動作、必要な権限、影響する会社・店舗・データの範囲
- 秘密値を除去したログや修正案

対象には認証・セッション、テナント／会社／店舗の分離、権限、入力検証、添付ファイル、会計・在庫の整合性を破る操作、セットアップ時の意図しない変更などを含みます。他者の環境への検証、実データの持ち出し、サービス妨害は行わないでください。

通常の不具合や改善提案は [Issues](https://github.com/adokoy001/DaifukuERP/issues) を利用できます。判断がつかない場合は非公開報告を選んでください。

## 開発用資格情報

`.env.example` と CI・テストにある既知の資格情報は、ローカルの合成データだけを扱うための公開 fixture です。本番設定には使いません。実環境の導入・更新は [安全なセットアップ手順](docs/operations/setup.md) に従い、`pnpm run setup` の計画と対象確認から始めてください。

## API の起動設定

| 設定 | 契約 |
| --- | --- |
| `HOST` | 既定は `127.0.0.1`。外部公開する場合は明示指定します。 |
| `PORT` | 十進整数の 1〜65535。既定は 3000。 |
| `JWT_SECRET` | 改行・NUL を拒否。`NODE_ENV=production` または非ループバック `HOST` では 32 文字以上・8 種類以上の文字が必要で、既知の開発用値も拒否します。長さ検証だけで乱数性を保証しないため、セットアップが生成するランダム値を使ってください。 |
| `CORS_ORIGINS` | カンマ区切りの厳密な http(s) origin のみ。ワイルドカード・path・query・fragment・資格情報を拒否します。 |
| CORS 未指定時 | production は許可 origin なし。開発時は `http://localhost:5173`、`http://127.0.0.1:5173`、`http://[::1]:5173` のみ。 |
| 起動エラー | 設定項目名と要件を返し、DB 接続 URL を含みうるドライバの生エラーは表示しません。 |

CORS は認証やネットワーク制御の代わりにはなりません。TLS 終端、通信・ログインの rate limit、添付ファイルのマルウェア検査は導入環境側の構成が必要です。実サービスの負荷と遠隔 TLS 接続は未検証です。
